---
export const partial = true;
export const prerender = true;

const response = await fetch('https://dog-api.kinduff.com/api/facts?number=1');
const responseData = await response.json();
const dogFact = responseData.facts[0];
---

{dogFact}